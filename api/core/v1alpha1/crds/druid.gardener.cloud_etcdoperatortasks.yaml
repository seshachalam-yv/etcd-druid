---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: etcdoperatortasks.druid.gardener.cloud
spec:
  group: druid.gardener.cloud
  names:
    kind: EtcdOperatorTask
    listKind: EtcdOperatorTaskList
    plural: etcdoperatortasks
    singular: etcdoperatortask
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EtcdOperatorTask represents an out-of-band operator task resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the EtcdOperatorTask resource.
            properties:
              config:
                description: |-
                  Config defines the configuration for the EtcdOperatorTask.
                  Only one of the configurations can be specified at a time.
                properties:
                  onDemandSnapshotConfig:
                    description: OnDemandSnapshotConfig specifies configuration for
                      on-demand snapshot tasks.
                    properties:
                      isFinal:
                        description: IsFinal indicates if this is the final snapshot.
                          Only applicable for full snapshots.
                        type: boolean
                      timeoutSeconds:
                        description: TimeoutSeconds is the timeout in seconds for
                          the snapshot operation.
                        format: int32
                        type: integer
                      type:
                        description: 'Type specifies the type of snapshot: "full"
                          or "delta".'
                        enum:
                        - full
                        - delta
                        type: string
                    required:
                    - type
                    type: object
                type: object
                x-kubernetes-validations:
                - message: The config field in spec is immutable and cannot be changed
                    after creation.
                  rule: self == oldSelf
              etcdRef:
                description: |-
                  EtcdRef refers to the name and namespace of the corresponding
                  Etcd owner for which the task has been invoked.
                properties:
                  name:
                    description: Name is the name of the Etcd resource.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Etcd resource.
                    type: string
                required:
                - name
                - namespace
                type: object
                x-kubernetes-validations:
                - message: The etcdRef field in spec is immutable and cannot be changed
                    after creation.
                  rule: self == oldSelf
              ttlSecondsAfterFinished:
                default: 3600
                description: |-
                  TTLSecondsAfterFinished is the time-to-live (in seconds) to garbage collect the
                  related resource(s) of the task once it has been completed.
                format: int32
                minimum: 1
                type: integer
                x-kubernetes-validations:
                - message: The ttlSecondsAfterFinished field in spec is immutable
                    and cannot be changed after creation.
                  rule: self == oldSelf
            required:
            - config
            type: object
          status:
            description: Status is the most recently observed status of the EtcdOperatorTask
              resource.
            properties:
              initiatedAt:
                description: InitiatedAt is the time at which the task has moved from
                  "pending" state to the inProgress state.
                format: date-time
                type: string
              lastErrors:
                description: LastErrors represents the errors when processing the
                  task.
                items:
                  properties:
                    code:
                      description: Code is an error code that uniquely identifies
                        an error.
                      type: string
                    description:
                      description: Description is a human-readable message indicating
                        details of the error.
                      type: string
                    observedAt:
                      description: ObservedAt is the time at which the error was observed.
                      format: date-time
                      type: string
                  required:
                  - code
                  - description
                  - observedAt
                  type: object
                maxItems: 10
                type: array
              lastOperation:
                description: LastOperation captures the last operation status if task
                  involves many stages.
                properties:
                  description:
                    description: Description is a human readable message indicating
                      details about the last operation.
                    type: string
                  lastTransitionTime:
                    description: LastTransitionTime records the timestamp of the most
                      recent state transition, marking when the operation moved from
                      its previous state to the current value specified in the 'State'
                      field.
                    format: date-time
                    type: string
                  phase:
                    description: Phase represents the current phase of the operation
                      with respect to the interface methods.
                    type: string
                  state:
                    description: State is the status of the last operation, one of
                      pending, progress, completed, failed.
                    type: string
                required:
                - description
                - phase
                - state
                type: object
              lastTransitionTime:
                description: LastTransitionTime is the last time the task transitioned
                  from one state to another.
                format: date-time
                type: string
              state:
                description: State is the last known state of the task.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
